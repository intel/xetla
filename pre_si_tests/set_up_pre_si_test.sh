#!/usr/bin/env bash
# clone drivers.gpu.compute.worklaods repo so this repo can use its testing framework
git clone https://github.com/intel-sandbox/drivers.gpu.compute.workloads.git drivers_gpu_compute_workloads --depth 1
